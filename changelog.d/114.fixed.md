- A pull request heading reading `## Also filed, not fixed` closed the issue named under it (#114).
  GitHub parses the *rendered* body, so the heading's trailing word and the next line's `**#114**`
  read as `fixed #114` across the boundary — a closing reference nobody typed, on an issue whose
  whole purpose was to record something this release does **not** fix.
- Compatibility: compatible - a tracker state and one changelog sentence. No code (#114).
- Reopened, with the cause on the issue. Found by the release audit's composition pass, which read
  the tracker's *state* rather than the issue text; the tag would otherwise have published a tracker
  saying a POSIX/Windows divergence was fixed in 1.0.1 when nothing in the delta touches that arm.
  The sibling `#113` one paragraph earlier survived the same heading, which is exactly why this
  looked like it had worked (#114).
